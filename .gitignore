pi/.pi/agent/
.env

# Environment files and secrets. Keep example templates trackable.
**/.env
**/.env.*
!.env.example
!**/.env.example

# OpenCode service auth / machine-local state — never commit.
opencode/.config/opencode/service.json

# Stray log files.
**/*.log

# Installed JavaScript dependencies are not dotfiles.
**/node_modules/
